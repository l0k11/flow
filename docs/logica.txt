HACERLO CON SERVIDOR
Tareas del servidor:
    - Escuchar nuevas conexiones
    - Dirigir los mensajes
    - Emitir estado (En línea - Desconectado)
    - Log
    - Guardar base de datos (Opcional)

Tareas del cliente:
    - Enviar JSON
    - Encriptar mensajes
    - Guardar base de datos:
        - ID = Uno que yo le pongo
        - Contactos (IP y Nombre (nombre personalizado))
        - Mensajes antiguos
        - 


Cada cliente tiene un id, al conectarse al servidor, actualiza la IP teniendo en cuenta ese ID

Cuando se manda un mensaje a un cliente desconectado, se almacena en una base de datos que se consulta cuando se conectarse